/**
 * Shared video constants for the backend pipeline.
 * Frontend code should import FPS from features/video-editor-v2/constants.ts instead.
 */

/** Project-wide frames per second — matches video rendering and MG composition fps. */
export const PROJECT_FPS = 24;
