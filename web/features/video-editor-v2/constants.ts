/**
 * Global constants for the video editor
 */

// Video composition settings
export const FPS = 30; // Frames per second for video playback and rendering
export const COMP_NAME = "VideoComposition"; // Name of the Remotion composition

// Image duration settings
export const DEFAULT_IMAGE_DURATION_FRAMES = 150; // 5 seconds at 30 FPS
export const IMAGE_DURATION_PERCENTAGE = 0.8; // 80% of timeline by default

// Default overlays configuration
export const DEFAULT_OVERLAYS = [];
