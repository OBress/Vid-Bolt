# MMM (Melodic Metadata Massacrer) Dependencies
# Install with: pip install -r requirements.txt
# Requires Python 3.10-3.13 (librosa doesn't support 3.14+ yet)

# Core dependencies
numpy>=1.20.0
scipy>=1.7.0
librosa>=0.10.0
soundfile>=0.12.0
mutagen>=1.45.0

# CLI
click>=8.0.0
rich>=13.0.0

# GPU acceleration (optional but recommended)
# cupy-cuda12x
# torch>=2.0.0
# torchaudio>=2.0.0

# Install MMM itself
# pip install -e git+https://github.com/geeknik/mmm.git#egg=mmm
